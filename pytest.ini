[pytest]
minversion = 7.0
addopts = -ra -q
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers = 
    integration: marks tests as integration tests
    unit: marks tests as unit tests
